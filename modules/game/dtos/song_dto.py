from dto.base_dto import Base

class Song(Base):
    title: str
    # anime: str
    # spotify_id: str TODO: i need to add this column for each song so I don't have to search before starting each game.
